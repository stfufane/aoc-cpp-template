#pragma once

#include "../src/DaySolver.h"

namespace adventofcode
{
    class Day@DAY@ : public DaySolver {
    public:
        Day@DAY@() : DaySolver(@DAY@) {}
        ~Day@DAY@() override = default;

        long solve(Part part, bool is_test) override;
    };
}